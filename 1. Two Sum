class Solution:
    def twoSum(a, b, c):
        d = {} 

        for i, num in enumerate(b):
            diff = c - num
            if diff in d:
                return [d[diff], i]
            d[num] = i
